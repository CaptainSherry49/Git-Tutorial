*) git init = "initializing Git"
*) git status = "Checking Status"
*) git add . = "Staging File"
*) git commit -m "Commit msg" = "Commiting File"
*) git checkout -f = "match all file with last commit"
*) git checkout "file name" = "Match that file with last commit"
*) git log = "Showing Commit History"
*) git log -p -3 = "Showing last 3 commit"
*) git diff = "Compare working directory to staging area"
*) git diff --staged = "Compare staging area with the last commit"
*) git commit -a -m = "Skipping Staging area"
*) git rm --cached "file name" = "remove file from staging area but not from PC"
*) git rm "File name" = "Remove file from PC also"
*) git branch "branch name" = "Creating Branch"
*) git branch = "Showing all branches"
*) git checkout "branch name" = "Switiching branch"
*) git branch merge "branch name" = "Merging branch"
*) git checkout -b "branch name" = "Creating New branch & switching to 
it"
*) git remote add origin "link" = "Connecting local repo with github"
*) git remote = "Showing origin"
*) git remote -v = "Showing remote feature"
*) git Push origin master = "Pushing repo on server"
*) git clone "link" = "Cloning repo from server"